const firstname: string = 'SpongeBob';
const species: string = 'sea sponge';
const age: number = 12;
const license: boolean = false;

// license.indexOf('SquarePants');

let characters: string[];
characters = ['SpongeBob', 'Peter', 'Sandy'];
characters.push('Plankton');

// characters = [1, 2, 3];
// characters.push(true);

function square(num: number): number {
  return num * num;
}

square(9);
// square(firstname);
// square(false);
